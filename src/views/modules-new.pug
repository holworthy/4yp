doctype html
html(lang="en")
	head
		include includes/head.pug
	body
		include includes/header.pug
		main
			h2 Create a module
			form(method="post")
				label(for="name") Name:
				input(type="text", name="name")
				label(for="code") Code:
				input(type="text", name="code")
				input.m1-top(type="submit", value="Create")