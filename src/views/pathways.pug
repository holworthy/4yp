doctype html
html(lang="en")
	head
		include includes/head.pug
	body 
		include includes/header.pug
		main
			h1.m1-bottom Pathways
			.pathways
				- for(let i = 0; i < pathways.length; i++)
					a.pathway(href="/pathways/"+pathways[i].id)
						h2 #{pathways[i].name}
				- if(pathways.length == 0)
					p No pathways exist yet!
			if !user.isStudent
				a.m1-top(href="/pathways/new")
					h2 Or Create A New Pathway 